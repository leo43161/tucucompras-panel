RewriteEngine On
RewriteBase /

# Existing files or dirs → servir directo
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback a index.html (SPA)
RewriteRule ^ index.html [L]